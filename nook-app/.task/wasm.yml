version: '3'

tasks:
  wasm:build:
    desc: Build nook-wasm for the web app in Docker.
    deps:
      - setup
    cmds:
      - task: docker:task
        vars: { TASK: _wasm:build }

  wasm:build:fast:
    desc: Build nook-wasm for the mounted web app without wasm-opt (local iteration).
    cmds:
      - task: docker:wasm:build:fast

  wasm:build:prod:
    desc: Build nook-wasm for the web app with optimized production wasm.
    cmds:
      - task setup WASM_BUILD_MODE=prod
      - task: docker:task
        vars:
          TASK: _wasm:build
          WASM_BUILD_MODE: prod

  _wasm:build:
    desc: Build nook-wasm for the web app when sources changed (skips if image-seeded pkg is current).
    cmds:
      - task: _wasm:build:run

  _wasm:build:fast:
    desc: Alias for _wasm:build (default dev mode skips wasm-opt).
    cmds:
      - task: _wasm:build

  _wasm:build:run:
    internal: true
    cmds:
      - >-
        sh -c 'out="{{.WEB_ROOT}}/src/lib/nook-wasm/nook_wasm_bg.wasm";
        stamp="{{.WEB_ROOT}}/src/lib/nook-wasm/.wasm-source-sha256";
        current="$(cd "{{.APP_ROOT}}" && find "nook-wasm/src" "nook-core/src" "nook-core/locales" "nook-auth/src" \
          \( -name "*.rs" -o -name "*.json" \) -print0 \
          | LC_ALL=C sort -z \
          | xargs -0 sha256sum \
          | sha256sum \
          | cut -d" " -f1)";
        case "${WASM_BUILD_MODE:-{{.WASM_BUILD_MODE}}}" in
          prod) wasm_opt_flag="" ; stamp_mode="optimized" ;;
          dev) wasm_opt_flag="--no-opt" ; stamp_mode="no-opt" ;;
          *) echo "Unsupported WASM_BUILD_MODE=${WASM_BUILD_MODE:-{{.WASM_BUILD_MODE}}} (expected dev or prod)" >&2; exit 1 ;;
        esac;
        desired="$current $stamp_mode";
        stored="";
        if [ -f "$stamp" ]; then stored="$(cat "$stamp")"; fi;
        if [ ! -f "$out" ] || [ "$desired" != "$stored" ]; then
          cd "{{.APP_ROOT}}" && wasm-pack build nook-wasm --target web --out-dir ../nook-web/src/lib/nook-wasm --out-name nook_wasm $wasm_opt_flag;
          echo "$desired" > "$stamp";
        fi'

  wasm:test:browser:
    desc: Run nook-wasm wasm-bindgen tests in headless Chrome (needs chromedriver).
    cmds:
      - cd "{{.APP_ROOT}}" && wasm-pack test --chrome --headless nook-wasm
