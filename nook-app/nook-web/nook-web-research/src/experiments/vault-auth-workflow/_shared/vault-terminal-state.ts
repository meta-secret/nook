export enum VaultTerminalStep {
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Name = 'name',
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Total = 'total',
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Threshold = 'threshold',
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  DeviceName = 'device-name',
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  PublicKey = 'public-key',
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Confirm = 'confirm',
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Sealed = 'sealed',
}

export enum VaultTerminalLineTone {
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Muted = 'muted',
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Success = 'success',
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Error = 'error',
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Answer = 'answer',
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Accent = 'accent',
}
