export enum JoinEnrollmentDialogVariant {
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  NeedsRequest = "needs_request",
  /** @public Used from Svelte templates; Knip cannot trace enum members there. */
  Pending = "pending",
}
