[package]
name = "nook-authenticator-domain"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Portable authenticator domain values shared by Nook core and companion WASM."
repository.workspace = true

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
thiserror = "2.0.18"

[dev-dependencies]
serde_json = "1.0.151"

[lints]
workspace = true
