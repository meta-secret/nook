# Focused, manually dispatched development tasks on the persistent Nook runner pool.
#
# This workflow is an allowlisted remote execution surface, not an arbitrary
# credential-bearing shell. Complete merge validation remains in pr.yml and is
# explicitly requested with task pr:validate.

name: Remote task

on:
  workflow_dispatch:
    inputs:
      task:
        description: Allowlisted Taskfile command
        required: true
        type: choice
        options:
          - preflight
          - rust:test
          - rust:lint
          - rust:coverage
          - wasm:build
          - wasm:test
          - wasm:test:browser
          - web:check
          - web:test
          - web:build
          - web:e2e
          - extension:check
          - extension:e2e
          - hive:verify
          - check
          - ci:pr
          - ci:pr:e2e

permissions:
  actions: read
  contents: read

concurrency:
  group: remote-${{ github.ref }}-${{ inputs.task }}
  cancel-in-progress: false

env:
  CI: true
  DOCKER_IMAGE: nook-web:remote-${{ github.run_id }}-${{ github.run_attempt }}
  DOCKER_E2E_IMAGE: nook-web-e2e:remote-${{ github.run_id }}-${{ github.run_attempt }}
  DOCKER_RUST_IMAGE: nook-rust:remote-${{ github.run_id }}-${{ github.run_attempt }}
  DOCKER_RUST_FAST_IMAGE: nook-rust-fast:remote-${{ github.run_id }}-${{ github.run_attempt }}
  DOCKER_RUST_BROWSER_IMAGE: nook-rust-browser:remote-${{ github.run_id }}-${{ github.run_attempt }}
  DOCKER_MKCERT_IMAGE: nook-mkcert:remote-${{ github.run_id }}-${{ github.run_attempt }}

jobs:
  preflight:
    if: inputs.task == 'preflight'
    name: Remote / preflight
    runs-on: nook
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task preflight

  rust-test:
    if: inputs.task == 'rust:test'
    name: Remote / rust:test
    runs-on: nook
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task rust:test

  rust-lint:
    if: inputs.task == 'rust:lint'
    name: Remote / rust:lint
    runs-on: nook
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task rust:lint

  rust-coverage:
    if: inputs.task == 'rust:coverage'
    name: Remote / rust:coverage
    runs-on: nook
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task rust:coverage

  wasm-build:
    if: inputs.task == 'wasm:build'
    name: Remote / wasm:build
    runs-on: nook
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task wasm:build

  wasm-test:
    if: inputs.task == 'wasm:test'
    name: Remote / wasm:test
    runs-on: nook
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task wasm:test

  wasm-test-browser:
    if: inputs.task == 'wasm:test:browser'
    name: Remote / wasm:test:browser
    runs-on: nook
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task wasm:test:browser

  web-check:
    if: inputs.task == 'web:check'
    name: Remote / web:check
    runs-on: nook
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task web:check

  web-test:
    if: inputs.task == 'web:test'
    name: Remote / web:test
    runs-on: nook
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task web:test

  web-build:
    if: inputs.task == 'web:build'
    name: Remote / web:build
    runs-on: nook
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task web:build

  web-e2e:
    if: inputs.task == 'web:e2e'
    name: Remote / web:e2e
    runs-on: nook
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: >-
          task web:test:e2e
          E2E_ARTIFACT_DIR=${{ runner.temp }}/nook-e2e-artifacts
      - name: Preserve Playwright diagnostics
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: remote-web-e2e-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/nook-e2e-artifacts
          if-no-files-found: warn
          retention-days: 15

  extension-check:
    if: inputs.task == 'extension:check'
    name: Remote / extension:check
    runs-on: nook
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task extension:check

  extension-e2e:
    if: inputs.task == 'extension:e2e'
    name: Remote / extension:e2e
    runs-on: nook
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: >-
          task extension:test:e2e
          E2E_ARTIFACT_DIR=${{ runner.temp }}/nook-e2e-artifacts
      - name: Preserve Playwright diagnostics
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: remote-extension-e2e-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/nook-e2e-artifacts
          if-no-files-found: warn
          retention-days: 15

  hive-verify:
    if: inputs.task == 'hive:verify'
    name: Remote / hive:verify
    runs-on: nook
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task hive:verify

  check:
    if: inputs.task == 'check'
    name: Remote / check
    runs-on: nook
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task check

  ci-pr:
    if: inputs.task == 'ci:pr'
    name: Remote / ci:pr
    runs-on: nook
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: task ci:pr

  ci-pr-e2e:
    if: inputs.task == 'ci:pr:e2e'
    name: Remote / ci:pr:e2e
    runs-on: nook
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2
      - run: >-
          task ci:pr:e2e
          E2E_ARTIFACT_DIR=${{ runner.temp }}/nook-e2e-artifacts
      - name: Preserve Playwright diagnostics
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: remote-ci-pr-e2e-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/nook-e2e-artifacts
          if-no-files-found: warn
          retention-days: 15
