# Workflow runtime libraries

These Node/Bun-compatible modules are loaded directly by GitHub workflow and
action steps. They stay close to the workflow runtime rather than being part
of repository domain tooling or the generic CI adapter directory.
