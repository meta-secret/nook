name: Connect Nook Rust cache
description: Materialize the direct TLS Redis credential as a runner-local file.

inputs:
  cache-redis-password:
    description: Password for the persistent remote Rust compiler cache.
    required: false
    default: ""
runs:
  using: node24
  main: main.js
