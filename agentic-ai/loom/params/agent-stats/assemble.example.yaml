agentStats:
  assemble:
    prNumber: 123
    # Resolves under OS temp with a stable task-anchor commit and opaque worktree ID.
    scratchPath: "{agentTempDir}/pr-123-scratch.json"
    outputPath: "{agentTempDir}/123.yaml"
    includeTestInventory: false
