githubConfigUrl: https://github.com/meta-secret/nook
githubConfigSecret: nook-arc-github

runnerScaleSetName: nook-k0s-container
minRunners: 0
maxRunners: 20

# Container jobs become ordinary pods through runner-container-hooks. The
# lifecycle-hook variant needs neither a Docker daemon nor shared RWX storage.
containerMode:
  type: kubernetes-novolume

controllerServiceAccount:
  namespace: arc-systems
  name: nook-arc-gha-rs-controller

listenerTemplate:
  spec:
    nodeSelector:
      nook.nokey.sh/node-role: control-storage
    tolerations:
      - key: nook.nokey.sh/arc-build
        operator: Equal
        value: preparing
        effect: NoSchedule
    containers:
      - name: listener
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          seccompProfile:
            type: RuntimeDefault
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: "1"
            memory: 512Mi

template:
  metadata:
    labels:
      nook.nokey.sh/role: arc-container-runner
  spec:
    nodeSelector:
      nook.nokey.sh/arc-build: "true"
    # The novolume container hook creates and watches the per-job pod through
    # the chart-owned, namespace-scoped service account.
    automountServiceAccountToken: true
    enableServiceLinks: false
    terminationGracePeriodSeconds: 60
    containers:
      - name: runner
        image: ghcr.io/actions/actions-runner:2.336.0@sha256:0cfdcc701ce933c6d243c6b0b2da767366dc9f2e99961d4c3754b0b78084cdda
        command: ["/home/runner/run.sh"]
        env:
          - name: ACTIONS_RUNNER_REQUIRE_JOB_CONTAINER
            value: "true"
          - name: ACTIONS_RUNNER_KUBERNETES_NAMESPACE
            value: arc-runners
          - name: ACTIONS_RUNNER_CONTAINER_HOOK_TEMPLATE
            value: /etc/nook-arc-hook/content.yaml
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          runAsNonRoot: true
          runAsUser: 1001
          runAsGroup: 1001
          seccompProfile:
            type: RuntimeDefault
        volumeMounts:
          - name: nook-arc-hook
            mountPath: /etc/nook-arc-hook
            readOnly: true
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 1Gi
    volumes:
      - name: nook-arc-hook
        configMap:
          name: nook-arc-container-hook
